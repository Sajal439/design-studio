{
  "buildCommand": "cd packages/database && npx prisma generate && cd ../.. && npx turbo run build",
  "installCommand": "npm install",
  "framework": "nextjs",
  "outputDirectory": "apps/web/.next"
}
