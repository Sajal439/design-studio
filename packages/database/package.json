{
  "name": "@repo/database",
  "version": "0.0.1",
  "private": true,
  "main": "./index.ts",
  "types": "./index.ts",
  "prisma": {
    "seed": "tsx seed.ts"
  },
  "scripts": {
    "db:generate": "prisma generate",
    "generate": "npm run db:generate",
    "db:migrate": "prisma migrate dev",
    "db:push": "prisma db push",
    "db:seed": "tsx seed.ts",
    "db:generate:design-manifest": "tsx scripts/generate-design-manifest.ts",
    "db:import:designs": "tsx scripts/import-designs.ts",
    "check-types": "tsc --noEmit",
    "db:studio": "prisma studio"
  },
  "dependencies": {
    "@prisma/client": "^6.6.0",
    "cloudinary": "^2.5.0",
    "p-limit": "^3.1.0",
    "prisma": "^6.6.0"
  },
  "devDependencies": {
    "tsx": "^4.19.0"
  }
}
